#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

uint32_t getUint32_t(float);
float getFloat(uint32_t);
uint32_t fsqrt(uint32_t);
uint32_t finv(uint32_t);
int is_valid_fsqrt( uint32_t );
int is_valid_finv( uint32_t );
